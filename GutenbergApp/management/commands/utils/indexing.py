# TODO: en utilisant hash_table_indexing, on va ouvrir chaque livre 1 à 1,
# compter le nombre d'occurences de chaque mots (il y a une fonction pour ça),
# remplir petit à petit l'hash table et la sérialiser (elle sera déserialiser au moment de la recherche simple)